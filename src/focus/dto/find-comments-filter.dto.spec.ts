import { FindCommentsFilterDto } from './find-comments-filter.dto';

describe('FindCommentsFilterDto', () => {
  it('should be defined', () => {
    expect(new FindCommentsFilterDto()).toBeDefined();
  });
});
