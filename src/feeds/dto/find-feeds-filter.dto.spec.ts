import { FindFeedsFilterDto } from './find-feeds-filter.dto';

describe('FindCommentsFilterDto', () => {
  it('should be defined', () => {
    expect(new FindFeedsFilterDto()).toBeDefined();
  });
});
